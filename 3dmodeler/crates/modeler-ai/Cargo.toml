[package]
name = "modeler-ai"
version = "0.1.0"
edition = "2021"
description = "AI provider abstraction: chat + tool use + model catalogs, transport-agnostic"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
