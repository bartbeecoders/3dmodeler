[x] When in front view (1), show the main zero grid lines

[x] when a reference image is selected, the orange selection box overlays the right panel

[x] increase the speed of the force increase (left mouse click in physics mode)
[ ] Add the force value (inccrease , max %) to the preferences screen


[x] add an option to apply the scale of an object (like in blender - apply scale)


Modify the bevel tool:
User selects edge
Ctrl-B start bevel mode
User drags to set bevel amount
User middle mouse scroll to set the amount of segments


Nice, it works.
Now give bevel and loopcut lines (temp lines) a different color than the regular lines
Also add to the toolbar and indication wether we are in vertex, edge or face mode (3 icons ?)