[package]
name = "img2model"
version = "0.1.0"
edition = "2021"
description = "Proof of concept: turn an image into a 3D model (GLB) using Microsoft TRELLIS.2"

[dependencies]
clap = { version = "4", features = ["derive"] }
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
